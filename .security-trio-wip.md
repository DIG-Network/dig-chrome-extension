# WIP — Security/Provider/Signing trio (#73 + #74 + #75)

Branch: `feat/security-provider-signing`. Single active dev lane (concurrency=1).
This file is a resume marker; it is removed before the PR merges.

## Tickets
- **#73** harden inpage↔content↔background provider channel: multiplexed, id/nonce-correlated,
  origin-validated end-to-end. Malformed dropped; concurrent multiplexed streams never cross;
  responses correlated to request+origin. Passes a spoofing/misrouting test suite.
- **#74** address-poisoning defenses + trusted address book: confusable/lookalike warning on
  every Send (prefix/suffix match, different middle), first-time-recipient surfacing, address
  book first-class (add-on-send, label counterparties).
- **#75** signing-request hardening for foreign/dApp spends: verify each required signer maps to a
  wallet-derived key; surface unaccountable signers; scrutinize mixed wallet+non-wallet spends;
  never sign a decode-failed spend (explicit user messaging).

## Sequence (guarantees landings under cap)
#73 fully (test→impl→push) → #74 → #75. Push after each ticket. PR opened (draft) after #73 green.

## Progress
- [x] branch skeleton pushed
- [x] #73 — src/lib/provider-channel.ts (+27 tests); wired into dig-provider.entry.ts + content.ts
- [ ] #74
- [ ] #75
